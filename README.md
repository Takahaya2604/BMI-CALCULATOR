# BMI-CALCULATOR
📊 BMI Calculator (Python)

A simple command-line Body Mass Index (BMI) Calculator built using Python.
This project calculates BMI based on user input and categorizes the result into standard health ranges.

🚀 Project Overview

This is a beginner-friendly Python project that demonstrates:

User input handling

Mathematical calculations

Conditional statements

Basic program structure

The application runs in the terminal/command line and requires no external libraries.

🧮 BMI Formula
BMI = Weight (kg) / (Height (m) × Height (m))

🛠 Built With

Python 3

No external dependencies
